{
  "name": "style-guide-skill",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend code style and style guide packaged as an AI skill",
  "scripts": {
    "build": "node build.js",
    "check": "node --check build.js && node build.js"
  },
  "engines": {
    "node": ">=18"
  }
}
